import { SpinnerBall } from '@phosphor-icons/react/dist/ssr';

export const Spinner = () => {
  return <SpinnerBall className="animate-bounce text-gray-700" size={48} />;
};
Spinner.displayName = 'Spinner';
